{
  "name": "tup-tup",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "bun run --filter '*' test",
    "build": "bun run --filter @tup-tup/cli build"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.4",
    "@types/bun": "latest",
    "@types/node": "^25.3.0"
  }
}
